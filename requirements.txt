mediapipe==0.10.32  # 确切版本：新版 .so 被 AMFI 拒绝→SIGKILL
opencv-python==4.13.0.92  # 同上（4.14 也被杀，范围约束不管用）
numpy==2.4.2  # 同上
Pillow==12.1.1  # 同上
mcp>=1.0.0
rumps; sys_platform == "darwin"
pyobjc-framework-Quartz; sys_platform == "darwin"  # 深度休眠读键鼠空闲
pystray; sys_platform == "win32"
winotify; sys_platform == "win32"
pyttsx3; sys_platform == "win32"
